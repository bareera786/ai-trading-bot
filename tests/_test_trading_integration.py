"""Legacy/trial trading integration tests.

This file is intentionally prefixed with an underscore so pytest will ignore it
during normal test collection. It exists to preserve the legacy test content for
later consolidation/migration into the `tests/integration/` tree.
"""

# Intentionally left as legacy fixture storage for future consolidation.

# NOTE: The full test suite should not collect this module.
