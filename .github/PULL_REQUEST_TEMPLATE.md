<!--
Short PR template to document optional integration runs.
Maintainers can add the `run-integration` label to a PR to trigger the opt-in integration test workflow.
-->

### What changed

Please describe your changes briefly.

---

Optional: add the label `run-integration` to trigger the integration test workflow (runs longer, opt-in).
