# Copy this file to config/deploy.env (ignored by git) and fill in your VPS settings.
# Values defined here will be exported automatically by scripts/deploy_to_vps.sh.
#
# VPS ssh settings
VPS_HOST=vps.example.com
VPS_USER=aibot
VPS_PATH=/home/aibot/ai-bot
# VPS_PORT=22

# Optional overrides
# VPS_SERVICE=aibot.service
# SKIP_RESTART=0
# RSYNC_DELETE=0
# SYNC_STATE=0
# SYNC_MODELS=0

# Marketing analytics (optional Plausible-style snippet)
# ENABLE_MARKETING_ANALYTICS=0
# MARKETING_ANALYTICS_SRC=https://plausible.io/js/script.js
# MARKETING_ANALYTICS_DOMAIN=ai-bot.example
# MARKETING_ANALYTICS_API_HOST=https://plausible.io

# Binance compliance & safety
# BINANCE_CREDENTIAL_KEY=base64_fernet_key_here   # e.g. python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# BINANCE_TERMS_ACCEPTED=0                        # Set to 1 only after accepting https://developers.binance.com/docs/binance-api/terms

# Compression Settings
ENABLE_ZSTD_COMPRESSION=1
ZSTD_COMPRESSION_LEVEL=3  # 1-22, 3=balanced
BACKUP_RETENTION_DAYS=30
MAX_MODEL_SNAPSHOTS=10

# Backup Schedule
STATE_BACKUP_INTERVAL=6  # hours
FULL_BACKUP_INTERVAL=24  # hours
