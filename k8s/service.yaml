apiVersion: v1
kind: Service
metadata:
  name: trading-bot-service
  labels:
    app: trading-bot
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: trading-bot